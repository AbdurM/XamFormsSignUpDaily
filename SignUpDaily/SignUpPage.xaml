<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SignUpDaily.CustomControls"
             mc:Ignorable="d"
             x:Class="Pages.SignUpPage">
    <ContentPage.Content>

        <StackLayout BackgroundColor="#D73532">

            <StackLayout.Resources>
                <Style TargetType="local:BorderlessEntry">
                    <Setter Property="Margin" Value="30,30,0,0"/>
                    <Setter Property="BackgroundColor" Value="#D73532"/>
                    <Setter Property="FontSize" Value="Small"/>
                    <Setter Property="PlaceholderColor" Value="White"/>
                </Style>

            </StackLayout.Resources>

            <Image HeightRequest="150"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   BackgroundColor="White"
                   Source="pinterest-icon"
                   Margin="0,100,0,30"/>

            <!--Sign up area-->
            <Label
                Text="Sign up"
                TextColor="White"
                FontSize="Title"
                FontAttributes="Bold"
                Margin="30"/>

            <local:BorderlessEntry Placeholder="Name"/>
            
            
            <local:BorderlessEntry Placeholder="Email"/>
            
            <local:BorderlessEntry Placeholder="Password"
                   IsPassword="True" />

            <Button Text="Sign up"
                    TextColor="#D73532"
                    FontSize="Large"
                    CornerRadius="25"
                    Margin="0,25,0,0"
                    WidthRequest="150"
                    HeightRequest="50"
                    HorizontalOptions="Center"
                    BackgroundColor="White"/>

            <Label Text="-Or-"
                   Margin="0,30,0,30"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   TextColor="White"
                   FontSize="10"/>
            
            <!--Fb and Google Buttons-->
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">

                <StackLayout.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Padding" Value="7"/>
                        <Setter Property="CornerRadius" Value="12"/>
                        <Setter Property="HeightRequest" Value= "40"/>
                        <Setter Property="WidthRequest" Value="150"/>
                        <Setter Property= "HorizontalOptions" Value="Start"/>
                    </Style>
                </StackLayout.Resources>

                <Button ImageSource="FbIcon"
                        Margin="0,0,10,10"
                        BackgroundColor="#0182C4"/>

                <Button ImageSource="GoogleIcon"
                        Margin="0,0,0,10"
                        BackgroundColor="White"/>


            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>